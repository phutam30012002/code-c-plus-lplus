#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    cin .ignore();
    while(t--)
    {
        string s;
        cin >> s;
        multiset<char> s1;
        for(int i=1;i< s.size();i++)
        {
            s1.insert(s[i]);
        }
        string s2;
        s2[0]=s[0];
        for(int i=0;i<s.size();i++)
        {
          
        }
    }
}